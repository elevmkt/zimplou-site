---
---

<section class="relative overflow-hidden bg-gradient-to-b from-emerald-50 via-white to-white py-20 sm:py-32">
  <!-- Background decoration -->
  <div class="absolute inset-0 overflow-hidden">
    <div class="absolute -top-40 -right-40 h-80 w-80 rounded-full bg-emerald-100/50 blur-3xl"></div>
    <div class="absolute -bottom-40 -left-40 h-80 w-80 rounded-full bg-teal-100/50 blur-3xl"></div>
  </div>

  <div class="relative mx-auto max-w-6xl px-4 sm:px-6 lg:px-8">
    <div class="text-center">
      <!-- Badge -->
      <div class="mb-6 inline-flex items-center gap-2 rounded-full bg-emerald-100 px-4 py-1.5 text-sm font-medium text-emerald-700">
        <span class="relative flex h-2 w-2">
          <span class="absolute inline-flex h-full w-full animate-ping rounded-full bg-emerald-400 opacity-75"></span>
          <span class="relative inline-flex h-2 w-2 rounded-full bg-emerald-500"></span>
        </span>
        Novo: Agendamento por WhatsApp com IA
      </div>

      <!-- Headline -->
      <h1 class="mx-auto max-w-4xl text-4xl font-bold tracking-tight text-slate-900 sm:text-6xl lg:text-7xl">
        Seu negócio no
        <span class="bg-gradient-to-r from-emerald-500 via-teal-500 to-cyan-500 bg-clip-text text-transparent">
          piloto automático
        </span>
      </h1>

      <!-- Subheadline -->
      <p class="mx-auto mt-6 max-w-2xl text-lg text-slate-600 sm:text-xl">
        Automatize agendamentos, confirmações e lembretes pelo WhatsApp. 
        Sua recepcionista virtual com IA trabalha 24/7 para você nunca perder um cliente.
      </p>

      <!-- CTA Buttons -->
      <div class="mt-10 flex flex-col items-center gap-4 sm:flex-row sm:justify-center">
        <a 
          href="https://app.zimplou.com" 
          class="inline-flex items-center justify-center rounded-full bg-gradient-to-r from-emerald-500 to-teal-500 px-8 py-4 text-lg font-semibold text-white shadow-xl shadow-emerald-500/25 transition hover:shadow-emerald-500/40 hover:scale-105"
        >
          Começar grátis por 14 dias
          <svg class="ml-2 h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M13 7l5 5m0 0l-5 5m5-5H6" />
          </svg>
        </a>
        <a 
          href="#how-it-works" 
          class="inline-flex items-center gap-2 text-slate-600 transition hover:text-emerald-600"
        >
          <svg class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M14.752 11.168l-3.197-2.132A1 1 0 0010 9.87v4.263a1 1 0 001.555.832l3.197-2.132a1 1 0 000-1.664z" />
            <path stroke-linecap="round" stroke-linejoin="round" d="M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
          </svg>
          Ver como funciona
        </a>
      </div>

      <!-- Social proof -->
      <div class="mt-16 flex flex-col items-center gap-4">
        <p class="text-sm text-slate-500">Feito para quem trabalha com agendamentos</p>
        <div class="flex flex-wrap items-center justify-center gap-6 text-slate-400">
          <span class="text-sm font-medium">🏥 Clínicas</span>
          <span class="text-sm font-medium">💇 Salões</span>
          <span class="text-sm font-medium">🎸 Estúdios</span>
          <span class="text-sm font-medium">🎓 Escolas</span>
          <span class="text-sm font-medium">🧘 Consultórios</span>
        </div>
      </div>
    </div>
  </div>
</section>
