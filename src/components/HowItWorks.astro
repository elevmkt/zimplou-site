---
const businessSteps = [
  {
    number: '1',
    title: 'Escolha seu ramo',
    time: '1 min',
    description: 'Salão, clínica, personal, tatuador... O sistema pré-configura serviços, preços e durações como sugestão. Personalize tudo como quiser.',
    icon: `<svg class="h-6 w-6" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
      <path stroke-linecap="round" stroke-linejoin="round" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4" />
    </svg>`,
  },
  {
    number: '2',
    title: 'Personalize a IA',
    time: '2 min',
    description: 'Escolha uma das 6 personalidades, dê um nome, ajuste o tom. Quer mais formal? Mais descontraída? Você decide.',
    icon: `<svg class="h-6 w-6" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
      <path stroke-linecap="round" stroke-linejoin="round" d="M11.049 2.927c.3-.921 1.603-.921 1.902 0l1.519 4.674a1 1 0 00.95.69h4.915c.969 0 1.371 1.24.588 1.81l-3.976 2.888a1 1 0 00-.363 1.118l1.518 4.674c.3.922-.755 1.688-1.538 1.118l-3.976-2.888a1 1 0 00-1.176 0l-3.976 2.888c-.783.57-1.838-.197-1.538-1.118l1.518-4.674a1 1 0 00-.363-1.118l-3.976-2.888c-.784-.57-.38-1.81.588-1.81h4.914a1 1 0 00.951-.69l1.519-4.674z" />
    </svg>`,
  },
  {
    number: '3',
    title: 'Conecte as contas',
    time: '5 min',
    description: 'WhatsApp (QR code), Google Calendar (1 clique), Stripe (opcional). Cada integração é independente - use só o que precisar.',
    icon: `<svg class="h-6 w-6" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
      <path stroke-linecap="round" stroke-linejoin="round" d="M13.828 10.172a4 4 0 00-5.656 0l-4 4a4 4 0 105.656 5.656l1.102-1.101m-.758-4.899a4 4 0 005.656 0l4-4a4 4 0 00-5.656-5.656l-1.1 1.1" />
    </svg>`,
  },
];

const clientJourney = [
  {
    step: 1,
    action: 'Cliente manda "oi" no WhatsApp',
    result: 'IA responde na hora, conversa natural',
  },
  {
    step: 2,
    action: 'Quer marcar? Recebe um link',
    result: 'Abre no celular, sem baixar app',
  },
  {
    step: 3,
    action: 'Escolhe horário, paga se quiser',
    result: 'Cartão, Pix, Google Pay, Apple Pay',
  },
  {
    step: 4,
    action: 'Pronto! Você recebe notificação',
    result: 'Evento já no seu Google Calendar',
  },
];
---

<section id="how-it-works" class="py-20 sm:py-32">
  <div class="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8">
    <!-- Section header -->
    <div class="text-center">
      <h2 class="text-3xl font-bold tracking-tight text-white sm:text-4xl">
        Configure em
        <span class="gradient-text">10 minutos</span>
        e deixe funcionando
      </h2>
      <p class="mt-4 text-lg text-slate-400 max-w-2xl mx-auto">
        Escolha o ramo, personalize a IA, conecte as contas. Depois é só trabalhar enquanto a IA cuida do resto.
      </p>
    </div>

    <!-- Business Setup Steps -->
    <div class="mt-16">
      <h3 class="text-center text-sm font-medium text-slate-500 uppercase tracking-wider mb-8">Sua configuração</h3>
      <div class="grid gap-8 lg:grid-cols-3">
        {businessSteps.map((step, index) => (
          <div class="relative">
            {/* Connector line */}
            {index < businessSteps.length - 1 && (
              <div class="absolute left-1/2 top-8 hidden h-0.5 w-full bg-gradient-to-r from-emerald-500/50 to-transparent lg:block"></div>
            )}

            <div class="relative flex flex-col items-center text-center">
              {/* Icon with number */}
              <div class="relative">
                <div class="flex h-16 w-16 items-center justify-center rounded-2xl bg-gradient-to-br from-emerald-500 to-teal-500 text-white shadow-lg shadow-emerald-500/25" set:html={step.icon}>
                </div>
                <div class="absolute -bottom-1 -right-1 flex h-6 w-6 items-center justify-center rounded-full bg-slate-900 text-xs font-bold text-emerald-400 ring-2 ring-emerald-500/50">
                  {step.number}
                </div>
              </div>

              <div class="mt-6">
                <h4 class="text-xl font-semibold text-white">{step.title}</h4>
                <span class="inline-block mt-1 text-xs font-medium text-emerald-400 bg-emerald-400/10 px-2 py-0.5 rounded-full">{step.time}</span>
              </div>
              <p class="mt-3 text-slate-400 text-sm">{step.description}</p>
            </div>
          </div>
        ))}
      </div>
    </div>

    <!-- Divider -->
    <div class="mt-20 flex items-center justify-center gap-4">
      <div class="h-px flex-1 bg-gradient-to-r from-transparent via-slate-700 to-transparent"></div>
      <span class="text-slate-500 text-sm">Daí em diante, é automático</span>
      <div class="h-px flex-1 bg-gradient-to-r from-transparent via-slate-700 to-transparent"></div>
    </div>

    <!-- Client Journey -->
    <div class="mt-12">
      <h3 class="text-center text-sm font-medium text-slate-500 uppercase tracking-wider mb-8">O que acontece quando cliente entra em contato</h3>

      <div class="glass-card p-8 max-w-3xl mx-auto">
        <div class="space-y-4">
          {clientJourney.map((item, index) => (
            <div class="flex items-center gap-4">
              {/* Step number */}
              <div class="flex-shrink-0 w-10 h-10 rounded-full bg-emerald-500/20 flex items-center justify-center text-emerald-400 font-bold">
                {item.step}
              </div>

              {/* Content */}
              <div class="flex-1 min-w-0">
                <p class="text-white font-medium">{item.action}</p>
                <p class="text-slate-400 text-sm">{item.result}</p>
              </div>

              {/* Arrow to next (except last) */}
              {index < clientJourney.length - 1 && (
                <div class="hidden sm:flex items-center text-slate-600">
                  <svg class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M13 7l5 5m0 0l-5 5m5-5H6" />
                  </svg>
                </div>
              )}
            </div>
          ))}
        </div>
      </div>
    </div>

  </div>
</section>
