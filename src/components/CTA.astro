---
---

<section class="py-20 sm:py-32">
  <div class="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8">
    <div class="relative overflow-hidden glass-card !rounded-3xl px-8 py-16 text-center sm:px-16 sm:py-24">
      <!-- Gradient overlay -->
      <div class="absolute inset-0 bg-gradient-to-br from-emerald-500/10 via-teal-500/10 to-cyan-500/10"></div>

      <!-- Background decoration -->
      <div class="absolute inset-0 overflow-hidden">
        <div class="absolute -top-20 -right-20 h-60 w-60 rounded-full bg-emerald-500/20 blur-3xl"></div>
        <div class="absolute -bottom-20 -left-20 h-60 w-60 rounded-full bg-teal-500/20 blur-3xl"></div>
      </div>

      <div class="relative">
        <!-- Benefit-focused headline -->
        <h2 class="text-3xl font-bold text-white sm:text-4xl lg:text-5xl">
          Pronto pra ter uma
          <span class="gradient-text">recepcionista 24h?</span>
        </h2>
        <p class="mt-4 text-lg text-slate-300 sm:text-xl max-w-2xl mx-auto">
          Configura em 10 minutos, conecta WhatsApp, deixa a IA cuidar do resto enquanto você trabalha.
        </p>

        <div class="mt-10 flex flex-col items-center gap-4 sm:flex-row sm:justify-center">
          <a
            href="https://app.zimplou.com"
            class="btn-glass btn-glass-primary text-lg"
          >
            Começar grátis
            <svg class="ml-2 h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round" d="M13 7l5 5m0 0l-5 5m5-5H6" />
            </svg>
          </a>
        </div>

        <p class="mt-6 text-sm text-slate-400">
          10 dias grátis. Sem cartão. Cancela quando quiser.
        </p>

        <!-- Quick benefits - Value focused -->
        <div class="mt-10 flex flex-wrap justify-center gap-6 text-sm text-slate-400">
          <div class="flex items-center gap-2">
            <svg class="h-4 w-4 text-emerald-400" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7" />
            </svg>
            <span>IA responde 24 horas</span>
          </div>
          <div class="flex items-center gap-2">
            <svg class="h-4 w-4 text-emerald-400" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7" />
            </svg>
            <span>Cliente não baixa app</span>
          </div>
          <div class="flex items-center gap-2">
            <svg class="h-4 w-4 text-emerald-400" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7" />
            </svg>
            <span>Você configura uma vez só</span>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
