---
const footerLinks = {
  produto: [
    { label: 'Recursos', href: '#features' },
    { label: 'Preços', href: '#pricing' },
    { label: 'Blog', href: '/blog' },
  ],
  empresa: [
    { label: 'Sobre', href: '/sobre' },
    { label: 'Contato', href: '/contato' },
  ],
  legal: [
    { label: 'Termos de uso', href: 'https://app.zimplou.com/pt-BR/legal' },
    { label: 'Privacidade', href: 'https://app.zimplou.com/pt-BR/legal#privacy' },
  ],
};
---

<footer class="relative py-16 border-t border-white/5">
  <div class="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8">
    <div class="grid gap-8 lg:grid-cols-4">
      <!-- Brand -->
      <div class="lg:col-span-1">
        <a href="/" class="flex items-center gap-3">
          <img src="/logo.svg" alt="Zimplou" class="h-10 w-10 rounded-xl" />
          <span class="text-xl font-bold text-white">Zimplou</span>
        </a>
        <p class="mt-4 text-sm text-slate-400">
          Agendamento inteligente com IA para pequenos negócios.
        </p>
      </div>

      <!-- Links -->
      <div class="lg:col-span-3">
        <div class="grid grid-cols-3 gap-8">
          <div>
            <h4 class="font-semibold text-white">Produto</h4>
            <ul class="mt-4 space-y-2">
              {footerLinks.produto.map((link) => (
                <li>
                  <a href={link.href} class="text-sm text-slate-400 transition hover:text-emerald-400">
                    {link.label}
                  </a>
                </li>
              ))}
            </ul>
          </div>
          <div>
            <h4 class="font-semibold text-white">Empresa</h4>
            <ul class="mt-4 space-y-2">
              {footerLinks.empresa.map((link) => (
                <li>
                  <a href={link.href} class="text-sm text-slate-400 transition hover:text-emerald-400">
                    {link.label}
                  </a>
                </li>
              ))}
            </ul>
          </div>
          <div>
            <h4 class="font-semibold text-white">Legal</h4>
            <ul class="mt-4 space-y-2">
              {footerLinks.legal.map((link) => (
                <li>
                  <a
                    href={link.href}
                    class="text-sm text-slate-400 transition hover:text-emerald-400"
                    target="_blank"
                    rel="noopener noreferrer"
                  >
                    {link.label}
                  </a>
                </li>
              ))}
            </ul>
          </div>
        </div>
      </div>
    </div>

    <!-- Bottom bar -->
    <div class="mt-12 border-t border-white/5 pt-8">
      <p class="text-center text-sm text-slate-500">
        {new Date().getFullYear()} Zimplou. Todos os direitos reservados.
      </p>
    </div>
  </div>
</footer>
