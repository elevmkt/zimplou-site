---
const footerLinks = {
  produto: [
    { label: 'Recursos', href: '#features' },
    { label: 'Preços', href: '#pricing' },
    { label: 'Blog', href: '/blog' },
  ],
  empresa: [
    { label: 'Sobre', href: '/sobre' },
    { label: 'Contato', href: '/contato' },
  ],
  legal: [
    { label: 'Termos de Uso', href: 'https://app.zimplou.com/pt-BR/legal' },
    { label: 'Politica de Privacidade', href: 'https://app.zimplou.com/pt-BR/legal#privacy' },
  ],
};
---

<footer class="relative py-20 border-t border-white/10">
  <div class="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8">
    <!-- Main footer content -->
    <div class="grid gap-12 lg:grid-cols-12">
      <!-- Brand section - more prominent -->
      <div class="lg:col-span-5">
        <a href="/" class="inline-flex items-center gap-4">
          <img src="/logo.svg" alt="Zimplou" class="h-14 w-14 rounded-2xl" />
          <span class="text-3xl font-bold text-white">Zimplou</span>
        </a>
        <p class="mt-6 text-base text-slate-400 max-w-sm">
          Agendamento inteligente com IA para pequenos negócios. Automatize seu atendimento pelo WhatsApp.
        </p>
        <div class="mt-6 flex items-center gap-4">
          <a
            href="https://app.zimplou.com"
            class="btn-glass btn-glass-primary !py-2 !px-5 !text-sm"
          >
            Comece gratis
          </a>
        </div>
      </div>

      <!-- Links -->
      <div class="lg:col-span-7">
        <div class="grid grid-cols-2 gap-8 sm:grid-cols-3">
          <div>
            <h4 class="text-sm font-semibold uppercase tracking-wider text-emerald-400">Produto</h4>
            <ul class="mt-4 space-y-3">
              {footerLinks.produto.map((link) => (
                <li>
                  <a href={link.href} class="text-sm text-slate-400 transition hover:text-white">
                    {link.label}
                  </a>
                </li>
              ))}
            </ul>
          </div>
          <div>
            <h4 class="text-sm font-semibold uppercase tracking-wider text-emerald-400">Empresa</h4>
            <ul class="mt-4 space-y-3">
              {footerLinks.empresa.map((link) => (
                <li>
                  <a href={link.href} class="text-sm text-slate-400 transition hover:text-white">
                    {link.label}
                  </a>
                </li>
              ))}
            </ul>
          </div>
          <div>
            <h4 class="text-sm font-semibold uppercase tracking-wider text-emerald-400">Legal</h4>
            <ul class="mt-4 space-y-3">
              {footerLinks.legal.map((link) => (
                <li>
                  <a
                    href={link.href}
                    class="text-sm text-slate-400 transition hover:text-white"
                  >
                    {link.label}
                  </a>
                </li>
              ))}
            </ul>
          </div>
        </div>
      </div>
    </div>

    <!-- Bottom bar with prominent legal links -->
    <div class="mt-16 border-t border-white/10 pt-8">
      <div class="flex flex-col items-center gap-4 sm:flex-row sm:justify-between">
        <p class="text-sm text-slate-500">
          &copy; {new Date().getFullYear()} Zimplou. Todos os direitos reservados.
        </p>
        <div class="flex items-center gap-6">
          <a
            href="https://app.zimplou.com/pt-BR/legal"
            class="text-sm text-slate-400 transition hover:text-emerald-400"
          >
            Termos de Uso
          </a>
          <a
            href="https://app.zimplou.com/pt-BR/legal#privacy"
            class="text-sm text-slate-400 transition hover:text-emerald-400"
          >
            Politica de Privacidade
          </a>
        </div>
      </div>
    </div>
  </div>
</footer>
